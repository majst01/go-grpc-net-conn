syntax = "proto3";

package testproto;

option go_package = "github.com/majst01/go-grpc-net-conn/testproto";

service TestService {
  rpc Stream(stream Bytes) returns (stream Bytes);
}

message Bytes {
  bytes data = 1;
}
